apiVersion: v1
kind: Pod
metadata:
  name: worker
  labels:
    app: devops-project
    tier: backend
    name: worker
spec:
  containers:
    - name: devops-project-worker
      image: jkole822/devops-project-worker
      envFrom:
        - secretRef:
            name: worker-secret